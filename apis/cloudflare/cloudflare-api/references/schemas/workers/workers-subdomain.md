# workers_subdomain

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `enabled` | boolean | Yes | Whether the Worker is available on the workers.dev subdomain. |
| `previews_enabled` | boolean | Yes | Whether the Worker's Preview URLs are available on the workers.dev subdomain. |

