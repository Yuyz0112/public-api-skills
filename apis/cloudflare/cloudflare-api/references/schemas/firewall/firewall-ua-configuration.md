# firewall_ua_configuration

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target` | enum: ua | No | The configuration target. You must set the target to `ua` when specifying a user agent in the rule. |
| `value` | string | No | the user agent to exactly match |

