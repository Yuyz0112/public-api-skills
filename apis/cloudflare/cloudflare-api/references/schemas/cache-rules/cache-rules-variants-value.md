# cache-rules_variants_value

Value of the zone setting.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `avif` | string[] | No | List of strings with the MIME types of all the variants that should be served for avif. |
| `bmp` | string[] | No | List of strings with the MIME types of all the variants that should be served for bmp. |
| `gif` | string[] | No | List of strings with the MIME types of all the variants that should be served for gif. |
| `jp2` | string[] | No | List of strings with the MIME types of all the variants that should be served for jp2. |
| `jpeg` | string[] | No | List of strings with the MIME types of all the variants that should be served for jpeg. |
| `jpg` | string[] | No | List of strings with the MIME types of all the variants that should be served for jpg. |
| `jpg2` | string[] | No | List of strings with the MIME types of all the variants that should be served for jpg2. |
| `png` | string[] | No | List of strings with the MIME types of all the variants that should be served for png. |
| `tif` | string[] | No | List of strings with the MIME types of all the variants that should be served for tif. |
| `tiff` | string[] | No | List of strings with the MIME types of all the variants that should be served for tiff. |
| `webp` | string[] | No | List of strings with the MIME types of all the variants that should be served for webp. |

