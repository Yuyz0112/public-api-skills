# zones_automatic_https_rewrites

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: automatic_https_rewrites | No | Turn on or off Automatic HTTPS Rewrites. |
| `value` | enum: on, off | No | The status of Automatic HTTPS Rewrites.
 |

