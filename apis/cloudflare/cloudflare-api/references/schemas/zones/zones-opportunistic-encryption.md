# zones_opportunistic_encryption

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: opportunistic_encryption | No | Opportunistic Encryption allows browsers to access HTTP URIs over an encrypted TLS channel.
It's not a substitute for HTTPS, but provides additional security for otherwise vulnerable requests.
 |
| `value` | enum: on, off | No | The status of Opportunistic Encryption.
 |

