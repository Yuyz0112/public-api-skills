# zones_ssl

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: ssl | No | Control options for the SSL feature of the Edge Certificates tab in the Cloudflare SSL/TLS app.
 |
| `value` | enum: off, flexible, full... | No | The encryption mode that Cloudflare uses to connect to your origin server.
 |

