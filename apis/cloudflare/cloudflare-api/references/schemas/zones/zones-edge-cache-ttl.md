# zones_edge_cache_ttl

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: edge_cache_ttl | No | Specify how long to cache a resource in the Cloudflare global
network. *Edge Cache TTL* is not visible in response headers.
 |
| `value` | integer | No |  |

