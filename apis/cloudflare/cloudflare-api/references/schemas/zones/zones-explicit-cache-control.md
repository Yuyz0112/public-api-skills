# zones_explicit_cache_control

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: explicit_cache_control | No | Origin Cache Control is enabled by default for Free, Pro, and
Business domains and disabled by default for Enterprise domains.
 |
| `value` | enum: on, off | No | The status of Origin Cache Control.
 |

