# zones_always_use_https

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: always_use_https | No | If enabled, any `http://`` URL is converted to `https://` through a
301 redirect.
 |

