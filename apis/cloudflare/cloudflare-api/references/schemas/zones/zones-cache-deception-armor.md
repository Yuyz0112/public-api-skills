# zones_cache_deception_armor

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: cache_deception_armor | No | Protect from web cache deception attacks while still allowing static
assets to be cached. This setting verifies that the URL's extension
matches the returned `Content-Type`.
 |
| `value` | enum: on, off | No | The status of Cache Deception Armor.
 |

