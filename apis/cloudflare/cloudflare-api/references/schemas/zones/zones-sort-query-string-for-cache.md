# zones_sort_query_string_for_cache

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | enum: sort_query_string_for_cache | No | Turn on or off the reordering of query strings. When query strings have the same structure, caching improves.
 |
| `value` | enum: on, off | No | The status of Query String Sort
 |

