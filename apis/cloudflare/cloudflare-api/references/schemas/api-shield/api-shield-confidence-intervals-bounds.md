# api-shield_confidence_intervals_bounds

Upper and lower bound for percentile estimate

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `lower` | number | No | Lower bound for percentile estimate |
| `upper` | number | No | Upper bound for percentile estimate |

