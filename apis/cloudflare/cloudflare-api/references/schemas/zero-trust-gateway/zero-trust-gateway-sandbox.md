# zero-trust-gateway_sandbox

Specify whether to enable the sandbox.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `enabled` | boolean | No | Specify whether to enable the sandbox. |
| `fallback_action` | enum: allow, block | No | Specify the action to take when the system cannot scan the file. |

