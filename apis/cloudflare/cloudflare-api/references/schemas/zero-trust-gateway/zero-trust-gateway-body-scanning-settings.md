# zero-trust-gateway_body-scanning-settings

Specify the DLP inspection mode.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `inspection_mode` | enum: deep, shallow | No | Specify the inspection mode as either `deep` or `shallow`. |

