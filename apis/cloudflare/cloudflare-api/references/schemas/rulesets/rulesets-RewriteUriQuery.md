# rulesets_RewriteUriQuery

A URI query rewrite.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `expression` | string | No | An expression that evaluates to a value to rewrite the URI query to. |
| `value` | string | No | A value to rewrite the URI query to. |

