# rulesets_RedirectFromList

A redirect based on a bulk list lookup.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `key` | string | Yes | An expression that evaluates to the list lookup key. |
| `name` | string | Yes | The name of the list to match against. |

