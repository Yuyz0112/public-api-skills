# rulesets_UrlNormalization

A URL Normalization object.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `scope` | enum: incoming, both, none | Yes | The scope of the URL normalization. |
| `type` | enum: cloudflare, rfc3986 | Yes | The type of URL normalization performed by Cloudflare. |

