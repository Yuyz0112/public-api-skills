# rulesets_RuleExposedCredentialCheck

Configuration for exposed credential checking.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `password_expression` | string | Yes | An expression that selects the password used in the credentials check. |
| `username_expression` | string | Yes | An expression that selects the user ID used in the credentials check. |

