# teams-devices_custom_s2s_config_request

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_client_id` | string | Yes | This id will be passed in the `CF-Access-Client-ID` header when hitting the `api_url`. |
| `access_client_secret` | string | Yes | This secret will be passed in the `CF-Access-Client-Secret` header when hitting the `api_url`. |
| `api_url` | string | Yes | The Custom Device Posture Integration  API URL. |

