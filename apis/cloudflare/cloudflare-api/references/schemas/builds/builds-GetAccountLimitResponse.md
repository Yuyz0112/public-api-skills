# builds_GetAccountLimitResponse

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `build_minutes_refresh_on` | string (date-time) | No | When build minutes will refresh (only for non-paid plans) |
| `has_reached_build_minutes_limit` | boolean | No | Whether build minutes limit has been reached (only for non-paid plans) |

