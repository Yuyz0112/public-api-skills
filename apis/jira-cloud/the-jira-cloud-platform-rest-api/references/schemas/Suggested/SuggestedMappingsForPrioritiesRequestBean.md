# SuggestedMappingsForPrioritiesRequestBean

Details of changes to a priority scheme's priorities that require suggested priority mappings.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `add` | integer[] | No | The ids of priorities being removed from the scheme. |
| `remove` | integer[] | No | The ids of priorities being removed from the scheme. |

