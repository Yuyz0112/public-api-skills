# webhook-ping-form-encoded

The webhooks ping payload encoded with URL encoding.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `payload` | string | Yes | A URL-encoded string of the ping JSON payload. The decoded payload is a JSON object. |

