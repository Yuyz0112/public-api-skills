# APIEntitiesGlqlField

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `key` | string | No | Unique field key |
| `label` | string | No | Human-readable field label |
| `name` | string | No | Underlying name of field, often the same as `key`, but it may be different if one type of field has multiple possible keys. Example `created` and `createdAt` |

