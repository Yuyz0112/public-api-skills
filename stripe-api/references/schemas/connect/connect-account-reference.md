# connect_account_reference

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `account` | any | No | The connected account being referenced when `type` is `account`. |
| `type` | enum: account, self | Yes | Type of the account referenced. |

