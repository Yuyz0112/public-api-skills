# radar_review_resource_session

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `browser` | string | No | The browser used in this browser session (e.g., `Chrome`). |
| `device` | string | No | Information about the device used for the browser session (e.g., `Samsung SM-G930T`). |
| `platform` | string | No | The platform for the browser session (e.g., `Macintosh`). |
| `version` | string | No | The version for the browser session (e.g., `61.0.3163.100`). |

