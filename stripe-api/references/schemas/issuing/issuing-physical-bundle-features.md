# issuing_physical_bundle_features

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `card_logo` | enum: optional, required, unsupported | Yes | The policy for how to use card logo images in a card design with this physical bundle. |
| `carrier_text` | enum: optional, required, unsupported | Yes | The policy for how to use carrier letter text in a card design with this physical bundle. |
| `second_line` | enum: optional, required, unsupported | Yes | The policy for how to use a second line on a card with this physical bundle. |

